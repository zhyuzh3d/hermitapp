# Hermit 1.1.0 界面与离线图标更新记录

日期：2026-09-11。版本：1.1.0 / versionCode 3。正式包名：io.github.zhyuzh3d.hermit。

本次完成黑白主题与彩色功能图标、应用库/图标库/设置三入口、应用分类搜索、简化卡片、分步导入、管理面板、危险操作确认、弹窗焦点和 Android 返回处理、键盘与系统栏避让。错误提示保留至用户关闭，操作进行中防重复触发；新增应用后重置筛选，避免新应用被旧搜索隐藏。修正了导入包名称覆盖用户明确填写名称的问题，GitHub 仓库名仅作最后的来源默认名。

Font Awesome Free 7.3.1 的 CSS、四个 WOFF2 字体及完整许可证已打包。目录包含 2,883 个图标/样式组合，无 Pro、无 CDN。在线与本地页面通过精确同源的公共资源网关使用；仅开放固定只读白名单。自动加载样式，支持标准 HTML 类名、显式 link、hermit.icons.create/load。在线 CSP 和 Service Worker 限制仍须由站点作者遵守，不绕过安全策略。

用户确认的“纯原生 HTML + JavaScript + CSS、无框架或构建前提、第三方最终静态产物中立接收”原则已写入根 AGENTS.md、README、设计文档、开发计划、SDK 说明和 docs/webapp-authoring.md。移除了当前方案中 esbuild、TypeScript 编译产物、必需 dist 目录等冲突措辞。examples/icon-starter/ 提供三个文件即可导入的原生示例。

## 验证证据

- `npm run check`、`npm test`（4/4）、`npm run build` 通过；后者仅作资源完整性检查，不执行前端编译。
- `:app:testDebugUnitTest` 1/1 通过；debug/release lint 0 error，现有风格和依赖版本 warning 保留。
- Samsung SM-S908U1 / Android 16 / API 36 上 `:app:connectedDebugAndroidTest` 24/24 通过。测试在独立 debug 测试沙箱运行，不操作用户的正式应用数据。
- 覆盖原有安装、文件、数据、备份、部署、信任隔离回归；新增实际 WebView 的自动字体加载、三类字体解码、图标搜索、深色主题 CSS、弹窗背景隔离、Android 返回、取消删除后实例仍存在；测试无框架、无前端构建。
- 在线 HTTP 页面在真实 WebView 中加载同源离线字体通过，测试服务器没有收到任何图标资源请求；公共资源网关另验证 HTTP/HTTPS/端口、字体签名、HEAD、路径穿越、未知路径、POST 与跨 Origin 拒绝边界。
- 正式 release 经 R8 构建、签名校验、manifest 身份/SDK/version/非 debuggable 校验通过，APK 内实际存在完整图标资源。
- 使用 `adb -d install -r` 从手机上的 1.0.0/code 2 覆盖到 1.1.0/code 3，返回 Success。首装时间仍为 2026-09-11 14:35:19，更新为 15:12:58，没有卸载或清空正式应用数据。
- 正式版冷启动 Status ok，TotalTime 383ms；本次启动检查未出现 AndroidRuntime 错误日志。此启动耗时是单次观测，不是性能基准。

签名证书 SHA-256 与旧正式包相同：`c0d882ae20bc5c79c2acc00b2f16b71f4f40be5ca9b9626a62d21477ccbf77c5`。

安装包：`artifacts/v1.1.0/hermit-v1.1.0-release.apk`，大小 1,233,331 字节。SHA-256：`d2412520e2fb57538e36a2ad3643e5348bf207c6331c1f3aa62881a34f29637d`。

源码来自基础提交 `9e0802b14132c334a45f1f073edebc05dd8208cd` 及本次工作区修改；本次未自动提交 Git。交付 manifest 明确标注 sourceTreeClean=false，不冒充干净提交构建，也未绕过要求干净源码的统一发布脚本。

## 留给用户的视觉验收

手机已打开更新后的正式应用。按用户要求，视觉审美由用户真机检查；本次没有用截图或浏览器视觉验收替代该步骤。建议检查已有应用卡片、添加面板及键盘、图标搜索/复制、深浅主题和管理确认流程。完整商用场景验收仍沿用既有实体设备清单，不因本次自动测试通过而标记全部完成。
